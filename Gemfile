source 'https://rubygems.org'

# Our markdown doesn't seem to conform to the linters provided in 0.12.0
gem 'mdl', '0.11.0'
gem 'rake'

# If you have any plugins, put them here!
group :jekyll_plugins do
  gem 'github-pages'
  gem 'jekyll-feed', '~> 0.6'
end
